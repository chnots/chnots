[package]
name = "chnots-server"
version = "0.1.0"
edition = "2024"

[dependencies]
## Config or Arguments
toml = "0.8.10"
clap = { version = "4.4.10", features = ["derive"] }

tokio = { workspace = true }
tokio-util = { workspace = true }

chnots-core = { path = "../backend/core" }
