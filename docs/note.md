# 2408-09

在实现后端过程中，发现一个很有用的工具 #[debug_handler]

用来调试接口为啥报错。

# 2508-13

将大多数的记录的问题都迁移到了专门的文件中，文件名称格式为 `yymm-dd-desc.md`。有几点说明一下

- 没有使用 org-mode 格式，既然整个 Chnots 应用都是支持的 markdown 格式，所以这里的任务管理使用了 markdown 格式。

- 正在进行中的任务放到 docs 目录下。

- 完成的任务迁移到 done 目录下。

- 该应用目前已经开始一年多了，但是很多问题没有记录出来，可能需要扒 git 历史。

该文件以后只记录一些不适合放到 git commit 记录和形成单独任务的文字。
